/**
 * @typedef {Object} EmojiItem
 * @property {string} name        e.g. "grinning face"
 * @property {string} category    category id
 * @property {string} group       subcategory from EmojiHub
 * @property {string[]} htmlCode  e.g. ["&#128512;"]
 * @property {string[]} unicode   e.g. ["U+1F600"]
 * @property {string} char        the actual emoji character, derived from htmlCode/unicode
 */

export {};

